# EAVユーザー管理システム フォルダ構成

## 📁 プロジェクト構成（最新）

```
HelloCSharp/
│
├── 📂 Areas/UserManagement/          # ユーザー管理機能（EAVモデル）
│   ├── Controllers/
│   │   ├── AttributeController.cs    # View返却用（React版HTMLを返す）
│   │   └── Api/
│   │       └── AttributeApiController.cs  # Web API（CRUD処理）
│   ├── Models/
│   │   ├── User.cs                   # ユーザーエンティティ
│   │   ├── AttributeDefinition.cs    # 属性定義エンティティ
│   │   └── UserAttributeValue.cs     # ユーザー属性値エンティティ
│   └── Views/
│       ├── _ViewImports.cshtml
│       ├── _ViewStart.cshtml
│       └── Attribute/
│           └── IndexReact.cshtml     # React版（メイン）
│
├── 📂 Controllers/                   # ルートコントローラー
│   └── HomeController.cs             # ホーム画面
│
├── 📂 Views/                         # ルートビュー
│   ├── _ViewImports.cshtml
│   ├── _ViewStart.cshtml
│   ├── Home/
│   │   └── Index.cshtml              # ホーム画面（アプリ説明）
│   └── Shared/
│       ├── _Layout.cshtml            # 共通レイアウト
│       ├── _ValidationScriptsPartial.cshtml
│       └── Error.cshtml
│
├── 📂 Scripts/                       # TypeScript/Reactソース
│   └── react/
│       ├── App.tsx                   # メインコンポーネント
│       ├── api.ts                    # API呼び出し
│       ├── types.ts                  # 型定義
│       └── components/
│           ├── AttributeTable.tsx    # 一覧テーブル
│           ├── AttributeModal.tsx    # 作成/編集モーダル
│           ├── Toast.tsx             # 通知
│           └── Loading.tsx           # ローディング
│
├── 📂 wwwroot/                       # 静的ファイル
│   ├── css/
│   │   └── site.css
│   └── js/
│       ├── site.js
│       └── react/
│           └── attribute-app.js      # ビルド済みReact
│
├── 📂 Data/                          # データベース関連
│   └── AppDbContext.cs               # EF Core DbContext
│
├── 📂 Migrations/                    # EF Core マイグレーション
│
├── 📂 docs/                          # ドキュメント
│   ├── README.md
│   ├── architecture/
│   ├── user-management/
│   └── notes/
│
├── Program.cs                        # エントリーポイント
├── HelloCSharp.csproj                # .NETプロジェクト設定
├── HelloCSharp.db                    # SQLiteデータベース
├── package.json                      # npm設定
├── tsconfig.json                     # TypeScript設定
└── README.md
```

---

## 🔄 データフロー（React + Web API）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              ブラウザ                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │ IndexReact.cshtml                                                      │ │
│  │   └─ <div id="react-root">                                            │ │
│  │   └─ <script src="/js/react/attribute-app.js">                        │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ↓                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │ Scripts/react/                                                         │ │
│  │   App.tsx → api.ts → fetch('/api/UserManagement/AttributeApi')        │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                              │ HTTP (JSON)
                              ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ASP.NET Core                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │ AttributeApiController.cs                                              │ │
│  │   [HttpGet]  GetAll()                                                  │ │
│  │   [HttpPost] Create()                                                  │ │
│  │   [HttpPut]  Update()                                                  │ │
│  │   [HttpDelete] Delete()                                                │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ↓                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │ AppDbContext.cs → SQLite (HelloCSharp.db)                             │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📂 各フォルダの役割

### Controllers/ vs Areas/.../Controllers/

| 場所 | 役割 | 例 |
|------|------|-----|
| **Controllers/** | 共通機能 | HomeController（トップページ） |
| **Areas/.../Controllers/** | 機能別 | AttributeController, UserController |
| **Areas/.../Controllers/Api/** | Web API | AttributeApiController |

### Scripts/react/

Reactのソースコード。esbuildでバンドルして`wwwroot/js/react/`に出力。

```bash
npm run build:dev   # 開発ビルド（sourcemap付き）
npm run build       # 本番ビルド（minify）
npm run watch       # ファイル監視
```

### Data/

Entity Framework CoreのDbContext。DIでControllerに注入される。

---

## 🌐 URLマッピング

| URL | Controller | 返却 |
|-----|------------|------|
| `/` | HomeController.Index | HTML（ホーム画面） |
| `/UserManagement/Attribute` | AttributeController.Index | HTML（React版） |
| `/api/UserManagement/AttributeApi` | AttributeApiController.GetAll | JSON |
| `/api/UserManagement/AttributeApi/5` | AttributeApiController.GetById | JSON |

---

## 📦 アーカイブ

学習初期のファイルは `../HelloCSharp_archive/` に移動済み：

```
HelloCSharp_archive/
├── Areas/Samples/           # 電卓・BMI（MVC学習サンプル）
├── Views/Attribute/         # 従来のMVC版View
├── Scripts/                 # 素のTypeScript版
├── wwwroot/js/              # 素のTS版JS
└── docs/                    # 学習初期のドキュメント
```
